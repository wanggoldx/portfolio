# Portfolio - Vite + React

## Commands

- `npm run dev` - Start dev server
- `npm run build` - Build to `dist/`
- `npm run lint` - ESLint check
- `npm run preview` - Preview build
- `npm run deploy` - Build + deploy to gh-pages

## Build Output

- Output dir: `dist/` (not `build/`)
- Deploy: `gh-pages -d dist`

## Project Structure

- Entry: `src/main.jsx` → `src/App.jsx`
- Pages: `src/pages/` (Home, About, Projects, Contact)
- Components: `src/components/`

## User Constraints

From global config (`~/.config/opencode/AGENTS.md`):
- Always ask before downloading anything
- Never delete files or code